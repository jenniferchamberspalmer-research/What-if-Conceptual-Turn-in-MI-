"""Extended Water Pattern Study harness.

This package layers a reproducible, parameterized study on top of the
original Water Pattern Tool WITHOUT modifying it. It reuses the unmodified
model-loading, embedding, probability, and SAE paths in `water_tool.core`
and `water_tool.views`; everything here is additive.

See water-pattern-study-design.md for the full specification.
"""
